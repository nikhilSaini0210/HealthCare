export enum Routes {
  Loader = '_loader',
  Splash = '_splash',
  MainApp = '_main_app',
  Login = '_login',
  Register = '_register',
  Home = '_home',
  Reminder = '_reminder',
  Pharmacy = '_pharmacy',
  Default = '_default',
  DefaultB = '_defaultb',
}
